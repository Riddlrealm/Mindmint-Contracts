[package]
name = "dynamic_nft"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "25.1.1", default-features = false }

[dev-dependencies]
soroban-sdk = { version = "25.1.1", features = ["testutils"] }
