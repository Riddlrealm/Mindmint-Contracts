[package]
name = "vesting"
version = "0.1.0"
edition = "2021"

[lib]
name = "vesting"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
