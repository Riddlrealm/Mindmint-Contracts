# DataKey migration plan (generated)

Generated by `scripts/generate_datakey_migration.py` for Issue #41 / ADR-0011.

Contracts using raw `symbol_short!` / `Symbol::new` storage keys are listed below with a proposed `DataKey` enum. Contracts that already declare a `DataKey` enum are marked compliant.

## achievement_collection — compliant (declares `DataKey`)

## achievement_nft — compliant (declares `DataKey`)

## achievement_sets — compliant (declares `DataKey`)

## airdrop_merkle_claim — compliant (declares `DataKey`)

## anti_bot — compliant (declares `DataKey`)

## auction — compliant (declares `DataKey`)

## batch_processing — compliant (declares `DataKey`)

## battle_pass — compliant (declares `DataKey`)

## bounty — compliant (declares `DataKey`)

## bridge — compliant (declares `DataKey`)

## charity_donation — compliant (declares `DataKey`)

## clawback_reward — compliant (no raw storage keys)

## community_grant — compliant (declares `DataKey`)

## completion_certificate — compliant (declares `DataKey`)

## conditional_reward — compliant (declares `DataKey`)

## crafting — compliant (declares `DataKey`)

## cross_contract — compliant (declares `DataKey`)

## daily_rewards — compliant (declares `DataKey`)

## decentralized_identity — compliant (declares `DataKey`)

## delegation — compliant (declares `DataKey`)

## dynamic_metadata — compliant (declares `DataKey`)

## dynamic_nft — compliant (declares `DataKey`)

## emergency_pause — compliant (declares `DataKey`)

## energy — compliant (declares `DataKey`)

## escrow — compliant (declares `DataKey`)

## event_logging — compliant (declares `DataKey`)

## event_ticket — compliant (declares `DataKey`)

## flash_challenge — compliant (declares `DataKey`)

## flash_loan — compliant (declares `DataKey`)

## fractional_nft — compliant (declares `DataKey`)

## gamification_rewards — compliant (declares `DataKey`)

## governance — compliant (declares `DataKey`)

## governance_token — compliant (declares `DataKey`)

## guild — compliant (declares `DataKey`)

## hint_marketplace — compliant (declares `DataKey`)

## identity — compliant (declares `DataKey`)

## immutable_leaderboard — compliant (declares `DataKey`)

## insurance — compliant (declares `DataKey`)

## insurance_pool — compliant (declares `DataKey`)

## leaderboard — compliant (declares `DataKey`)

## lending — compliant (declares `DataKey`)

## liquidity_mining — compliant (declares `DataKey`)

## liquidity_pool — compliant (declares `DataKey`)

## lottery — compliant (declares `DataKey`)

## loyalty_points — compliant (declares `DataKey`)

## marketplace — compliant (declares `DataKey`)

## mentorship — compliant (declares `DataKey`)

## multiplayer_match — compliant (declares `DataKey`)

## multisig_escrow — compliant (declares `DataKey`)

## multisig_treasury — compliant (declares `DataKey`)

## nft-leasing — compliant (declares `DataKey`)

## nft_bundle — compliant (declares `DataKey`)

## nft_composability — compliant (declares `DataKey`)

## nft_staking — compliant (declares `DataKey`)

## nft_trait_marketplace — compliant (declares `DataKey`)

## nft_upgrade — compliant (declares `DataKey`)

## nft_wrapper — compliant (declares `DataKey`)

## oracle — compliant (declares `DataKey`)

## oracle_integration — compliant (declares `DataKey`)

## oracle_price_feed — compliant (declares `DataKey`)

## payment_splitter — compliant (declares `DataKey`)

## player_sponsorship — compliant (declares `DataKey`)

## prediction_market — compliant (declares `DataKey`)

## prize_pool — compliant (declares `DataKey`)

## progressive_jackpot — compliant (declares `DataKey`)

## proof_of_activity — compliant (declares `DataKey`)

## proxy — compliant (declares `DataKey`)

## puzzle_bounty — compliant (declares `DataKey`)

## puzzle_co_creation — compliant (declares `DataKey`)

## puzzle_crowdfund — compliant (declares `DataKey`)

## puzzle_dao — compliant (declares `DataKey`)

## puzzle_factory — compliant (declares `DataKey`)

## puzzle_insurance — compliant (declares `DataKey`)

## puzzle_lottery — compliant (declares `DataKey`)

## puzzle_pool_staking — compliant (declares `DataKey`)

## puzzle_rental — compliant (declares `DataKey`)

## puzzle_subscription_tier — compliant (declares `DataKey`)

## puzzle_verification — compliant (declares `DataKey`)

## puzzle_voting — compliant (declares `DataKey`)

## quest — compliant (declares `DataKey`)

## quest_chain — compliant (declares `DataKey`)

## rbac — compliant (declares `DataKey`)

## referral — compliant (declares `DataKey`)

## referral_reward — compliant (no raw storage keys)

## reputation — compliant (declares `DataKey`)

## revenue_share — compliant (declares `DataKey`)

## reward_token — compliant (declares `DataKey`)

## reward_vault — compliant (declares `DataKey`)

## royalty_distribution — compliant (declares `DataKey`)

## royalty_splitter — compliant (declares `DataKey`)

## seasonal_event — compliant (declares `DataKey`)

## skill_badge — compliant (declares `DataKey`)

## skill_rating — compliant (declares `DataKey`)

## social_tipping — compliant (declares `DataKey`)

## social_wager — compliant (declares `DataKey`)

## staking — compliant (declares `DataKey`)

## staking_rewards — compliant (declares `DataKey`)

## subscription — compliant (declares `DataKey`)

## subscription_dao — compliant (declares `DataKey`)

## subscription_gift — compliant (declares `DataKey`)

## time_attack — compliant (declares `DataKey`)

## timelock_vault — compliant (declares `DataKey`)

## token_burn — compliant (declares `DataKey`)

## token_swap — compliant (declares `DataKey`)

## tournament — compliant (declares `DataKey`)

## tournament_prize_pool — compliant (declares `DataKey`)

## vesting — compliant (declares `DataKey`)

## vesting_schedule — compliant (declares `DataKey`)

## whitelist — compliant (declares `DataKey`)

## yield-aggregator — compliant (no raw storage keys)

## yield_farming — compliant (declares `DataKey`)

## zk_proof — compliant (declares `DataKey`)

**Summary:** 112 compliant, 0 affected.

