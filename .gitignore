# Rust
target/
# Cargo.lock is committed intentionally: pinning transitive deps (e.g.
# ed25519-dalek =2.2.0) is the only reliable way to keep CI reproducible
# for a workspace that does not use --locked in its CI commands.
**/*.rs.bk

# Soroban
.soroban/
*.wasm
*.optimized.wasm

# Test artifacts
test_snapshots/
**/test_snapshots/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
**/test_snapshots/
