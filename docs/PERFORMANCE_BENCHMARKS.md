# Performance benchmarks

Working notes on benchmark numbers.

## Methodology

- `soroban contract invoke` over loopback RPC.
- Warm 1000 invocations, then measure 10 000.
- Median is reported; p95 for tail.

## Reference timings

Re-run on every release and pin numbers here.

| Contract | p50 (ms) | p95 (ms) | Notes |
|---|---|---|---|
| (placeholder) | — | — | fill in from `bench/` runs |
