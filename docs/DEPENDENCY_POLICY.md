# Dependency policy

Adding a new dependency? Check:

- [ ] Maintained (last release within 12 months).
- [ ] License-compatible with our `MIT` LICENSE.
- [ ] Small attack surface.
- [ ] Doesn't bloat build times excessively.

Dependabot (`.github/dependabot.yml`) opens weekly PRs for upstream updates.
