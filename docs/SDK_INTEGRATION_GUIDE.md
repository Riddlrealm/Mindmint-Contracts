# SDK integration guide

Which Soroban SDK channel to choose and how to wire it into a host app.

| Host language | SDK | Notes |
|---|---|---|
| Rust | `soroban-sdk` 21.0.0 | first-class |
| TypeScript | `@stellar/stellar-sdk` | generated types |
| Python | `stellar-sdk` | bindings |
